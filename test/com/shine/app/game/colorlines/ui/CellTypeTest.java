package com.shine.app.game.colorlines.ui;

import junit.framework.Assert;

import org.junit.Test;

public class CellTypeTest {

	@Test
	public void test() {
		// Assert.assertTrue(CellType.T2.getImage() == CellType.T2.getImage());
		// Assert.assertSame(CellType.T2.getImage(), CellType.T2.getImage());
		// Assert.assertSame(CellType.T0.getImage(), CellType.T0.getImage());
		// Assert.assertNotSame(CellType.T2.getImage(), CellType.T3.getImage());
		// Assert.assertNotSame(CellType.T0.getImage(), CellType.T3.getImage());

	}

}
